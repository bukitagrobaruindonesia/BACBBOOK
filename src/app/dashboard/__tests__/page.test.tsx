import { describe, it, expect } from "vitest";
import { render, screen } from "@testing-library/react";

describe("Dashboard Page", () => {
  it("placeholder test for dashboard", () => {
    expect(true).toBe(true);
  });
});
